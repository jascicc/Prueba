@model IEnumerable<Estadias.Models.Registro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.alumno.nombreAlumno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.empresa.nombreEmpresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.alumno.nombreAlumno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empresa.nombreEmpresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.registroID }) |
            @Html.ActionLink("Details", "Details", new { id=item.registroID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.registroID })
        </td>
    </tr>
}

</table>
